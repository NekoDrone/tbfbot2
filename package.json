{
  "dependencies": {
    "@google-cloud/firestore": "^6.7.0",
    "@google-cloud/functions-framework": "^3.3.0",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "functions-framework --target=collectNewCaseData",
    "prestart": "npm run build",
    "gcp-build": "npm run build"
  }
}
